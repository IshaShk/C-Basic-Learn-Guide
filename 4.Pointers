#include <iostream>

using namespace std;
int main () {
   int  var1;
   char var2[10];

   cout << "Address of var1 variable: ";
   cout << &var1 << endl;// pointer to var1

   cout << "Address of var2 variable: ";
   cout << &var2 << endl;//pointer to var2

   return 0;
}
